/**
 * Top menu action labels
 * Used for clicking actions in toolbar / header menus
 *
 * NOTE:
 * - Values must exactly match UI visible text
 * - Case-sensitive
 * - Centralized to avoid hardcoded strings in tests
 */

export const MENU_OPTION = Object.freeze({
    SETUPS: 'Setups',
    NEW: 'New',
    VIEW: 'View',
    EDIT: 'Edit',
    REFRESH: 'Refresh',
    SAVE:'Save',
    APPROVE: 'Approve',
});